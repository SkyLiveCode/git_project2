// ฟังก์ชัน sum สำหรับการคำนวณผลรวมของสองตัวเลข
exports.sum = (a, b) => a + b;

// ฟังก์ชัน multiply สำหรับการคำนวณผลคูณของสองตัวเลข
exports.multiply = (a, b) => a * b;

// พื้นที่ใช้ทำฟังก์ชันคำนวณในอนาคต